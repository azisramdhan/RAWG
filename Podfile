# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RAWG' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RAWG
  pod 'SDWebImage', '~> 5.0'
  pod 'Cosmos', '~> 23.0'
  pod 'SwiftLint'

  target 'RAWGTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RAWGUITests' do
    # Pods for testing
  end

end
